# Copyright 2018 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# This file is used by the File Update Service which continuously
# checks for updates in the `triggerFile` listed here. If there is an update
# in the `triggerFile`, the service will
#   1. archive the new version of the `triggerFile` in its GCS bucket.
#   2. for each one of `extraFiles`, generate a new version of it and archive it
#      in its GCS bucket.

# Bazel debian package tarball.
- triggerFile:
    target: //bazel_layer:bazel_debs.tar
    bucket: container_release_tools_e2e_tests
    dir: bazel_docker_gcloud_staging/bazel/debs
    metadata:
        target: //bazel_layer:metadata.yaml
        bucket: container_release_tools_e2e_tests
        object: bazel_docker_gcloud_staging/bazel/metadata/debs/metadata.yaml

# Clang debian package tarball.
- triggerFile:
    target: //clang_layer:clang_debs.tar
    bucket: container_release_tools_e2e_tests
    dir: bazel_docker_gcloud_staging/clang/debs
    metadata:
        target: //clang_layer:metadata.yaml
        bucket: container_release_tools_e2e_tests
        object: bazel_docker_gcloud_staging/clang/metadata/debs/metadata.yaml

# Docker debian package tarball.
- triggerFile:
    target: //docker_layer:docker_debs.tar
    bucket: container_release_tools_e2e_tests
    dir: bazel_docker_gcloud_staging/docker/debs
    metadata:
        target: //docker_layer:metadata.yaml
        bucket: container_release_tools_e2e_tests
        object: bazel_docker_gcloud_staging/docker/metadata/debs/metadata.yaml

# Gcloud debian package tarball.
- triggerFile:
    target: //gcloud_layer:gcloud_debs.tar
    bucket: container_release_tools_e2e_tests
    dir: bazel_docker_gcloud_staging/gcloud/debs
    metadata:
        target: //gcloud_layer:metadata.yaml
        bucket: container_release_tools_e2e_tests
        object: bazel_docker_gcloud_staging/gcloud/metadata/debs/metadata.yaml

# Python debian package tarball.
- triggerFile:
    target: //python_layer:python_debs.tar
    bucket: container_release_tools_e2e_tests
    dir: bazel_docker_gcloud_staging/python/debs
    metadata:
        target: //python_layer:metadata.yaml
        bucket: container_release_tools_e2e_tests
        object: bazel_docker_gcloud_staging/python/metadata/debs/metadata.yaml
